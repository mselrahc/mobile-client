import React from 'react';
import CommonStack from '../common/CommonStack';
import { unitsRoutes } from '../../configs/routes';

function UnitsScreenStack() {
  return <CommonStack routes={unitsRoutes} />;
}

export default UnitsScreenStack;
